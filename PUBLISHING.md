# 공개 기준 (Publishing Policy)

이 저장소의 내용은 실무 경험에서 나왔습니다. 그래서 **무엇을 공개하지 않을지를 먼저 정하고** 시작했습니다.

## 기준을 한 번 고쳤습니다

처음 세운 기준은 **"이 저장소만 보고 회사를 찾아낼 수 있는가"** 였습니다.
적대적 검증을 돌려본 뒤 폐기했습니다. **통과할 수 없는 시험이었기 때문입니다.**

이력서에 링크를 붙이는 순간 이름이 공개되고, 이름이 공개되면 소속은 한 다리 건너입니다.
익명을 전제로 한 안전은 처음부터 성립하지 않았습니다.

그래서 기준을 바꿨습니다.

> **회사가 밝혀진 상태로, 그 회사 담당자가 이 문서를 읽어도 문제가 없는 문장만 남긴다.**

훨씬 엄격한 기준이고, 실제로 적용해보니 케이스 하나를 통째로 들어내야 했습니다.

## 남기는 것과 빼는 것

| 남김 | 뺌 |
|---|---|
| 내가 고안한 방법론·프레임워크 | 회사에서 작성한 코드·문서 원본 |
| 비율 · 배수 · 개선폭 · 성공률 | 금액 일체, 단가 관련 수치와 용어 |
| 문제 유형과 설계 판단 | 상호 · 브랜드 · 상품명 · 도메인 |
| 더미 데이터 재현 데모 | 실명 (동료 · 거래처 · 고객) |
| 아키텍처 역할 다이어그램 | 시스템 좌표 (저장소 · PR · 채널ID · 경로) |
| 안전장치 설계 원칙 | 자격증명 · 만료일 · 우회 방법 |
| — | **회사의 규제 이행 상태 · 손실 사고 · 현재 취약점** |

마지막 줄이 기준을 바꾸면서 새로 들어온 항목입니다.
이건 익명화로 해결되지 않습니다. 회사가 특정된 순간 **규제기관·경쟁사·고객이 그대로 인용할 수 있는 자료**가 되기 때문에, 가리는 게 아니라 **빼는 것**이 유일한 답입니다.

## 서술 규칙

**주어를 바꿉니다.** 회사에서 무슨 일이 있었는지가 아니라, 내가 무엇을 설계했는지를 씁니다.

```
✗  "정기 배치가 한 달간 실행되지 않아 규제 대응이 밀렸다"   ← 회사의 이행 상태
✓  "실행 부재를 사람이 인지하지 못하는 실패 유형을 정의하고,
    마지막 성공 시각의 나이를 감시하는 하트비트를 도입했다"   ← 내가 만든 것
```

**금액이 등장하면 그 문장은 실패입니다.** 규모는 건수·처리량 같은 비금전 단위로만 쓰고, 회사가 특정될 만큼 독특한 수치는 반올림합니다.

```
✗  자릿수까지 정확한 품목 수     ✓  "천여 품목 감사"
✗  "N/N 처리" 형태의 절대 건수   ✓  "대상 전량 처리, 실패 0건"
```

## 공개 전 자동 검사

```bash
# 규칙 파일은 저장소 폴더 밖에 둔다 (한 번만)
mkdir -p ~/.config/redaction
cp .redaction-rules.example ~/.config/redaction/portfolio-rules
chmod 600 ~/.config/redaction/portfolio-rules
# → 실제 값을 채운다. 한 행에 식별자 하나. 파이프로 묶지 않는다.

REDACTION_RULES_FILE=~/.config/redaction/portfolio-rules ./scripts/redaction-gate.sh
```

**규칙 파일을 저장소 안에 두지 않는 이유가 있습니다.** `.gitignore` 는 **커밋만** 막습니다.
폴더를 zip으로 압축하거나 백업·클라우드 동기화에 걸어두면 추적되지 않는 파일도 그대로 따라갑니다.
「가릴 문자열 전체 목록」에게 필요한 조건은 *추적되지 않는 것*이 아니라 **저장소 안에 없는 것**입니다.
게이트도 이걸 검사합니다 — 규칙 파일이 저장소 폴더 안에 있으면 실패시킵니다.
판정은 **파일명이 아니라 경로와 내용**으로 합니다. ① 지정된 규칙 파일 경로가 저장소 안이면 즉시 실패, ② 이름이 무엇이든 저장소 안에 「분류명 + 패턴」 목록 형태의 파일이 있으면 실패입니다.
저장소에 남아 있는 `.redaction-rules.example` 이 괜찮은 근거도 이름이 아니라 내용입니다 — 패턴 칸에 플레이스홀더만 있고 리터럴이 없습니다.

게이트를 설계하면서 **처음 만든 버전이 실패했습니다.** 금지어 목록을 스크립트에 직접 적었더니,
그 목록 자체가 정답표가 됐습니다. 게다가 그 게이트는 자기 자신을 검사 대상에서 제외하고 있어서
**유출을 담은 채로 "전 항목 통과"를 출력**했습니다.

그래서 다시 만들었습니다.

| 실패한 설계 | 고친 설계 |
|---|---|
| 금지어를 스크립트에 하드코딩 | 규칙을 **저장소 밖** 파일로 분리 (경로는 환경변수로 주입) |
| 한 줄에 파이프로 묶인 전체 목록 | 한 행 = 식별자 하나 (한 번의 열람 ≠ 전체 노출) |
| 자기 자신을 스캔에서 제외 | 검사 도구도 검사 대상 |
| 규칙이 없으면 통과 | 규칙이 없으면 **실패** |
| 적중한 값을 로그에 출력 | 위치만 출력 (로그도 유출 경로) |
| 규칙 파일을 파일명으로 식별 | 경로와 **내용**으로 판정 (이름을 바꿔도 걸린다) |
| 작업 트리만 스캔 | **커밋 히스토리 객체까지** 스캔 (얕은 클론이면 통과로 처리하지 않음) |

**「규칙이 없으면 실패」** 가 핵심입니다. 이 저장소가 [조용한 실패 5유형](docs/silent-failure-patterns.md)에서
정의한 **유형 3(무증상 미수행)** 을 제 안전장치가 그대로 재현했습니다.
검사할 목록이 없어서 아무것도 검사하지 않았는데 초록불이 떴습니다.

**작업 트리만 보는 검사는 통과로 처리하지 않습니다.** 파일에서 지운 문자열은 커밋 객체에 남고,
공개되는 것은 작업 트리가 아니라 저장소 전체입니다. 그래서 게이트는 `git rev-list --all --objects` 로
도달 가능한 객체를 열거해 `git cat-file` 로 블롭 내용까지 같은 규칙으로 스캔하고, 스캔한 객체 수를 출력합니다.
얕은 클론이면 히스토리를 볼 수 없으므로 실패시킵니다 (CI 는 `fetch-depth: 0`).

pre-commit 훅으로 걸어두려면 — 규칙 파일 경로를 함께 넘겨야 합니다:

```bash
printf '%s\n' '#!/usr/bin/env bash' \
  'REDACTION_RULES_FILE="$HOME/.config/redaction/portfolio-rules" exec ./scripts/redaction-gate.sh' \
  > .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit
```
